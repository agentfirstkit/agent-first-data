# macOS metadata files
.DS_Store
# hypha spawn working directory
.cmn
# Dependencies and build outputs
node_modules
typescript/dist
rust/target
python/build
python/dist
python/*.egg-info

# Python venv (auto-provisioned by test.sh when system python lacks deps)
.venv
